spring.application.name=school_bus
# 配置端口号为9090
server.port=9090

# 配置数据库
# 配置驱动
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
# 若连接的是云数据库则将localhost改为云端ip
spring.datasource.url=jdbc:mysql://sh-cynosdbmysql-grp-2gf1ewak.sql.tencentcdb.com:28451/school_bus?useUnicode=true&characterEncoding=UTF-8&useSSL=false&serverTimezone=Asia/Shanghai
# Mysql用户
spring.datasource.username=root
# Mysql对应用户密码
spring.datasource.password=Lmcindy6388

# MyBatis Configuration
mybatis.mapper-locations=classpath:mapper/*.xml
mybatis.type-aliases-package=com.lm.school_bus.entity
mybatis.configuration.map-underscore-to-camel-case=true

# Jackson Configuration (Date Format)
spring.jackson.date-format=yyyy-MM-dd HH:mm:ss
spring.jackson.time-zone=GMT+8

# Enforce UTF-8 for requests/responses to avoid garbled Chinese
server.servlet.encoding.charset=UTF-8
server.servlet.encoding.enabled=true
server.servlet.encoding.force=true
server.tomcat.uri-encoding=UTF-8
